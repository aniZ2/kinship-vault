rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ DEV ONLY: anyone on the internet can read/write anything
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
